# Frontend Service - Environment Variables
# Copy this file to .env.local and fill in actual values

# ============================================================================
# BACKEND API CONFIGURATION
# ============================================================================

# Backend API Base URL
# Development (local): http://localhost:8000
# Development (Docker): http://backend:8000
# Production: https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# ============================================================================
# AUTHENTICATION CONFIGURATION
# ============================================================================

# Better Auth Secret (MUST match JWT_SECRET in backend/.env)
# Generate with: openssl rand -base64 32
# IMPORTANT: This is the SAME value as backend JWT_SECRET
BETTER_AUTH_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# Better Auth URL (Frontend URL)
# Development: http://localhost:3000
# Production: https://yourdomain.com
BETTER_AUTH_URL=http://localhost:3000

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

# Environment (development, staging, production)
NEXT_PUBLIC_ENVIRONMENT=development

# Application Name
NEXT_PUBLIC_APP_NAME=Hackathon Todo

# ============================================================================
# SECURITY NOTES
# ============================================================================

# 1. NEVER commit .env.local to version control (.gitignore already excludes it)
# 2. BETTER_AUTH_SECRET MUST match backend JWT_SECRET exactly
# 3. Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# 4. Keep sensitive secrets (like BETTER_AUTH_SECRET) WITHOUT the NEXT_PUBLIC_ prefix
# 5. Use environment-specific URLs for NEXT_PUBLIC_API_URL in production

# ============================================================================
# DEVELOPMENT QUICK START
# ============================================================================

# 1. Copy this file: cp .env.local.example .env.local
# 2. Generate secret: openssl rand -base64 32
# 3. Paste the SAME secret into:
#    - backend/.env as JWT_SECRET
#    - frontend/.env.local as BETTER_AUTH_SECRET
# 4. Update NEXT_PUBLIC_API_URL if using Docker (http://backend:8000)

# ============================================================================
# OPENAI CONFIGURATION (Phase III)
# ============================================================================

# OpenAI Domain Key for production (not needed for localhost)
# Get from: https://platform.openai.com/settings/organization/security/domain-allowlist
# Leave empty for local development
NEXT_PUBLIC_OPENAI_DOMAIN_KEY=
