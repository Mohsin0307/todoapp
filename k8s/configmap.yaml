apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-chatbot-config
  namespace: todo-chatbot
data:
  # Add configuration values here
  config.json: |
    {
      "appName": "Todo Chatbot",
      "environment": "development"
    }